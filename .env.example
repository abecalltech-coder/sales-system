# --- Database ---
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sales_system?schema=public
DIRECT_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sales_system?schema=public

# --- Auth ---
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
COOKIE_SECRET=change-me-cookie-secret
ENCRYPTION_KEY=change-me-32-byte-key-for-aes256

# --- URLs ---
FRONTEND_URL=http://localhost:5173
API_URL=http://localhost:4000

# --- Google連携(Phase5で使用、未設定でも起動可能) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/api/integrations/google-calendar/callback
GOOGLE_CALENDAR_ID=
GOOGLE_WEBHOOK_SECRET=change-me-webhook-secret
GOOGLE_TOKEN_ENCRYPTION_KEY=change-me-32-byte-key

# --- Realtime ---
SOCKET_CORS_ORIGIN=http://localhost:5173
REDIS_URL=redis://localhost:6379

# --- Web Push 通知(商談リマインド・実施報告のデスクトップ/スマホ通知) ---
# `cd apps/api && node -e "console.log(require('web-push').generateVAPIDKeys())"` で生成した値を設定する。
# 未設定でもアプリは起動するが、プッシュ通知は無効(アプリ内のベル通知のみになる)。
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# --- 初期管理者 ---
ADMIN_EMAIL=admin@example.com
ADMIN_INITIAL_PASSWORD=ChangeMe123!

# --- その他 ---
TZ=Asia/Tokyo
NODE_ENV=development
PORT=4000
